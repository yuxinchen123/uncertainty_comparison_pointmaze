# device

What each graphics card class was measured to do: the probe script, the capacity calculation that turns a measurement into a copy count, one JSON per probed node, and a ledger table across node classes.

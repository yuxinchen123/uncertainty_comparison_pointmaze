# runs

Training run folders, named `YYYY-MM-DD-HH-MM_<slug>`. One run is one sweep and may span many jobs; the run-level `metrics.jsonl` and `summary.json` are always aggregates over the per-unit shards in `data/`.
